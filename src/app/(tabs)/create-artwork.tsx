// Placeholder tab route. The tab's `tabPress` is intercepted in the tabs layout
// to push the full-screen add-artwork wizard (`/artworks/new`) onto the stack,
// so this screen never actually renders.
export default function Screen() {
  return null;
}
